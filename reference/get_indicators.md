# Collate the indicator list into a data.frame

Collate the indicator list into a data.frame

## Usage

``` r
get_indicators(sc, pattern = ".*")
```

## Arguments

- sc:

  Scores object (as provided by `read_RESQUE`)

- pattern:

  regexp pattern that filters the results. The default selects all
  indicators
