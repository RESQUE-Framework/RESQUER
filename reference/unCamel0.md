# Replaces CamelCase in a vector with spaces between words

Replaces CamelCase in a vector with spaces between words

## Usage

``` r
unCamel0(x)
```

## Arguments

- x:

  A character vector which should be uncameled.
